{% extends '::base.html.twig' %}
{% block _body %}  <h2>Signos Vitales Suministrados</h2> {% endblock %}
{% block body %}
    
    <h2>Ver</h2>
    <br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ entity.valor }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre | title }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ entity.fecha|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('signosvitalessuministrados') }}">
            Regresar a la lista
        </a>
    </li>
    <li>
        <a href="{{ path('signosvitalessuministrados_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
