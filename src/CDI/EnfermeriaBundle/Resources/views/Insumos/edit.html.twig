{% extends '::base.html.twig' %}

 {% block stylesheets %}
    {{parent()}}
    <!-- Bootstrap datepicker core CSS -->
    <link rel="stylesheet" href="{{ asset('bundles/enfermeria/css/datepicker.css')}}"
{% endblock %}

{% block _body %}  <h2>Insumos</h2> {% endblock %}
{% block body %}
   
    <h2>Editar</h2>
    <br>

    
    {{ form_start(edit_form) }}
    {{ form_errors(edit_form) }}
    
    
     <div class="form-group">       
        {{ form_label(edit_form.tipoInsumo) }}
        {{ form_errors(edit_form.tipoInsumo) }}
            <div class="col-lg-4">
                {{ form_widget(edit_form.tipoInsumo) }}
            </div>
    </div>
 
    <div class="form-group">       
        {{ form_label(edit_form.nombre) }}
        {{ form_errors(edit_form.nombre) }}
            <div class="col-lg-4">
                {{ form_widget(edit_form.nombre) }}
            </div>
    </div>
    
    
    
    
     <div class="form-group">       
        {{ form_label(edit_form.stock) }}
        {{ form_errors(edit_form.stock) }}
            <div class="col-lg-4">
                {{ form_widget(edit_form.stock) }}
            </div>
    </div>
    
    
    
     <div class="form-group">       
        {{ form_label(edit_form.disponible) }}
        {{ form_errors(edit_form.disponible) }}
            <div class="col-lg-4">
                {{ form_widget(edit_form.disponible) }}
            </div>
    </div>

    <div class="form-group">
        <div class="col-lg-offset-2 col-lg-4">
          <button type="submit" class="btn btn-primary">Actualizar</button>
        </div>
  </div>
      
{{ form_end(edit_form) }}

        <ul class="table">
    <li>
        <a href="{{ path('insumos') }}">
            Regresar a la lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
